# API Tests Project

This project contains automated API tests using Pytest, Requests and Allure.
The project follows the Endpoint Object pattern as described in `PROMT.md`.

## Setup

Install dependencies:

```bash
pip install -r requirements.txt
```

Run tests with Allure report generation:

```bash
pytest
```
